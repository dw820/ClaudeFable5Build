{
  "name": "autocut",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Autonomous AI video editor — self-correction loop controller (task #1)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "demo": "tsx src/loop/demo.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
