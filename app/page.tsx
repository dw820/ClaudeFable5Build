// Placeholder — Lane 2 (UI) replaces this with the Setup screen
// (Library + BriefComposer) built from autocut-final.html.
export default function Page() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="font-serif text-2xl text-ink">AutoCut — scaffold ready</div>
    </main>
  );
}
